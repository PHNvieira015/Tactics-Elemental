using UnityEngine;

public class SpawningTile : MonoBehaviour
{
    // This can remain empty if you just need to mark the tile as a spawning location
    // Optionally, you can add more properties or methods specific to spawning logic if necessary
}